import { FC } from 'react'

import InnerLayout from '../layouts/inner/InnerLayout'

const SellPage: FC = () =>{
    return(
        <InnerLayout>
            <div>Hello, World!</div>
        </InnerLayout>
    );
}

export default SellPage;
