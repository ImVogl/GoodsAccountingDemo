import { FC } from 'react'

import WorkingArea from '../base/working/WorkingArea'

const SellPage: FC = () =>{
    return(
        <WorkingArea>
            <div>Hello, World!</div>
        </WorkingArea>
    );
}

export default SellPage;
